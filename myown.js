const posts = [
    { title: 'Post One', body: 'This is post one'},
    { title: 'Post Two', body: 'This is post two'}
];

const promise = createPost(posts);

promise.then(function() {
    console.log(createPost);
})


























